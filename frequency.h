#ifndef FREQUENCY_H
#define FREQUENCY_H

#include <stddef.h>

// Прототип функции для частотного теста
int frequency_test(const int *vector, size_t length, size_t block_size, double alpha);

#endif // FREQUENCY_H
